# name of static resource of bundled app.  If changed, make sure to update VF page script includes
resource_name=app

# sfdx org aliases or usernames to configure targets for npm scripts
#   Might not want to check these into .git, (to allow each developer to configure independently)
dev_alias=demo@bass.starter
scratch_alias=scratch
prod_alias=demo@bass.starter
